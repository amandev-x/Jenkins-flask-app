pipeline {
    agent any 
    options {
        skipDefaultCheckout true
    }
    stages {
        // Checkout Stage
        stage("Checkout Code") {
            steps {
                sh "git clone https://github.com/amandev-x/Jenkins-flask-app.git"
            }
        }

        // // Build Stage
        // stage() 
    }
}